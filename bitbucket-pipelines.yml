image: node:6
clone:
  depth: full
pipelines:
  default:
    - step:
        script:
          - npm i
          - npm i -g gulp gulp-cli
          - git push https://heroku:$HEROKU_API_KEY@git.heroku.com/$HEROKU_APP_NAME.git HEAD
          - gulp production